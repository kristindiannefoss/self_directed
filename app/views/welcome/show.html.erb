

<% if current_user %>
  Hello, <%= current_user.user_name %>
  <%= link_to 'Log Out', logout_path %>
<% else %>
  <%= link_to "Sign in with Facebook", facebook_login_path %>
  <%= render "fb_login" %>
<% end %>

<div
  class="fb-like"
  data-share="true"
  data-width="450"
  data-show-faces="true">
</div>
