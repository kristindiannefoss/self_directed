
<!-- Intro Header -->
<header class="intro">
    <div class="intro-body">
        <div class="container">
            <div class="row">
                <div class="col-md-8 col-md-offset-2">

                    <h1 class="brand-heading">Tough Love</h1>
                    <p class="intro-text">Because sometimes you need it.<br>Someone on your shoulder to tell it like it is.</p>

                    <% if current_user %>
                      <a href= "<%= dashboard_path(current_user) %>" class="btn btn-default btn-lg">Let's Do This</a>
                    <% else %>
                    <% end %>

                </div>
            </div>
        </div>
    </div>
</header>
